interface:
  display_name: "Spec Index"
  short_description: "Maintain spec indexes and prune old specs"
  default_prompt: "Use $spec-index to create or refresh a repository spec-index.md, delete old specs, and keep related spec documents aligned."
